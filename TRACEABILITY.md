# TRACEABILITY

- NA-0001 DONE — Attachment Service Runtime Implementation — completed by PR #2 with a single-node local-disk runtime that faithfully implements `DOC-CAN-006`, keeps plaintext off service surfaces, and establishes the minimal `rust` CI + main-branch protection baseline; see `NEXT_ACTIONS.md`.

- NA-0001 implementation — single-node local-disk runtime faithfully implements `DOC-CAN-006`: session create/upload/status/commit/abort/retrieval handlers, secret-bearing header carriage, local JSON journals plus opaque ciphertext part/object files, deterministic reject taxonomy, expiry/quota/abuse checks, audit-log redaction, deterministic integration tests, and a minimal `rust` CI lane — `Cargo.toml`; `src/lib.rs`; `src/main.rs`; `tests/service_contract.rs`; `tests/NA-0001_runtime_contract_faithfulness.md`; `.github/workflows/rust.yml`; `DECISIONS.md` — PR #2.

- NA-0002 implementation — repo-local docs/governance now freeze the current operational posture and the next hardening lane without changing runtime code: `README.md` and `START_HERE.md` state the single-node local-disk runtime truthfully, `docs/NA-0002_operational_hardening_contract.md` mirrors qsl-protocol `DOC-ATT-002`, the constrained-host validation ladder treats weak hosts and weak relays as first-class inputs, and the next implementation step is explicit — `README.md`; `START_HERE.md`; `docs/NA-0002_operational_hardening_contract.md`; `tests/NA-0002_operational_hardening_contract_evidence.md`; `DECISIONS.md`.

- NA-0002 DONE — qsl-attachments PR #4 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/4) promoted the repo-local docs/governance lane as the sole READY item, PR #5 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/5) froze the repo-local deployment / operational hardening contract without any runtime changes, and the queue now advances to repo-local operational hardening plus constrained-host validation — see `NEXT_ACTIONS.md`.

- NA-0003 implementation — runtime now enforces a storage-headroom reserve before accepting staged or committed attachment data, logs operator-safe startup configuration, raises the ciphertext ceiling just enough to carry the 100 MiB target class truthfully, and records constrained-host ladder evidence over the real relay with stage-by-stage saturation versus correctness classification — `Cargo.toml`; `src/lib.rs`; `src/main.rs`; `tests/service_contract.rs`; `tests/NA-0003_constrained_host_validation_evidence.md`; `README.md`; `START_HERE.md`; `DECISIONS.md`.

- NA-0003 DONE — qsl-attachments PR #7 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/7) merged the smallest runtime/ops hardening controls plus the constrained-host real-world validation evidence, and the repo-local lane now hands the next blocker back to qsl-protocol instead of creating another qsl-attachments-local READY item — see `NEXT_ACTIONS.md`.

- NA-0004 implementation — the stronger reference deployment is now reproducible on `qatt` without storing secrets: `docs/NA-0004_reference_deployment_runbook.md` records the local-build + binary-copy install path, systemd/Caddy layout, mandatory relay preflight, and first-install issues; `tests/NA-0004_reference_deployment_validation_evidence.md` records message-only, attachment-only, mixed, restart/resume, concurrency, short-soak, and threshold-ladder evidence over the real relay while distinguishing weak-relay saturation from attachment-service correctness — `README.md`; `START_HERE.md`; `docs/NA-0004_reference_deployment_runbook.md`; `tests/NA-0004_reference_deployment_validation_evidence.md`; `DECISIONS.md`.

- NA-0004 DONE — qsl-attachments PR #9 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/9) promoted the repo-local reference-deployment lane, PR #10 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/10) recorded the stronger `qatt` install/update path plus mixed message + attachment validation evidence, and the repo-local queue now returns to `READY=0` because the next blocker is broader stress/soak/chaos evidence upstream rather than another qsl-attachments-local follow-on — see `NEXT_ACTIONS.md`.

- NA-0005 implementation — the bounded kitchen-sink lane is now grounded without changing runtime semantics: `tests/NA-0005_stress_soak_chaos_evidence.md` records message-only, mixed, threshold, large-file, restart/resume, concurrency `2/4/8`, and `30` minute soak evidence across weak-host `qsl` and stronger reference-host `qatt`, while `README.md`, `START_HERE.md`, and `DECISIONS.md` keep the repo-local posture truthful about the new evidence and next decision boundary — `README.md`; `START_HERE.md`; `DECISIONS.md`; `tests/NA-0005_stress_soak_chaos_evidence.md`.

- NA-0005 DONE — qsl-attachments PR #12 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/12) promoted the repo-local bounded stress/soak/chaos lane, PR #13 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/13) recorded the bounded kitchen-sink evidence across weak-host `qsl` and stronger reference-host `qatt`, and the repo-local queue now returns to `READY=0` because no further qsl-attachments-local blocker outranks the upstream default-path / legacy decision — see `NEXT_ACTIONS.md`.

- NA-0006 implementation — passive audit-log events now minimize stable identifiers instead of recording raw `session_id`, `locator_ref`, or `attachment_id` values: `src/lib.rs` emits short deterministic audit handles while preserving ciphertext-only service semantics, and `tests/service_contract.rs` proves audit snapshots exclude secrets, plaintext markers, and the full stable identifiers — `src/lib.rs`; `tests/service_contract.rs`.

- NA-0006 DONE — qsl-attachments PR #15 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/15) promoted the repo-local metadata / secret-hygiene lane, PR #16 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/16) enforced passive audit-log identifier minimization without changing attachment-service semantics, and the repo-local queue now returns to `READY=0` because the next blocker is the upstream authn/authz / policy-subject contract rather than another qsl-attachments-local enforcement gap — see `NEXT_ACTIONS.md`.

- NA-0007 implementation — repo-local docs now freeze the authn/authz / policy-subject boundary explicitly: `docs/NA-0007_authn_authz_policy_subject_contract.md` defines the operator-scoped deployment as the sole current service policy subject, keeps `resume_token` / `fetch_capability` as per-resource authorizers rather than service identities, assigns deployment-global quota ownership to that operator-scoped subject, and limits legitimate operator-visible metadata to non-secret refs, deterministic handles, state, and reason codes without attachment-service semantic redesign — `README.md`; `START_HERE.md`; `DECISIONS.md`; `docs/NA-0007_authn_authz_policy_subject_contract.md`; `src/lib.rs`; `tests/service_contract.rs`; qsl-protocol `docs/canonical/DOC-CAN-006_QATT_Attachment_Service_Contract_v0.1.0_DRAFT.md`; qsl-protocol `docs/design/DOC-G5-004_Metadata_Leakage_Surface_Review_and_Logging_Contract_v0.1.0_DRAFT.md`.

- NA-0007 DONE — qsl-attachments PR #18 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/18) promoted the repo-local contract lane, PR #19 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/19) froze the operator-scoped authn/authz / policy-subject contract without changing attachment-service semantics, qsl-protocol PR #589 (https://github.com/QuantumShieldLabs/qsl-protocol/pull/589) recorded the cross-repo governance truth, and the repo-local queue now advances to `NA-0008` as the sole READY item because explicit implementation of the frozen contract is the next blocker rather than more finalization — see `NEXT_ACTIONS.md`.

- NA-0008 implementation — the frozen authn/authz / policy-subject contract is now explicit on runtime/operator surfaces without changing the wire contract: `src/lib.rs` exports `Config::operator_policy_surface()` / `AppState::operator_policy_surface()`, `src/main.rs` emits the same operator-safe markers at startup, and `tests/service_contract.rs` proves deployment-global quota ownership, many-transfer behavior under one deployment policy/quota, and one-capability-per-resource semantics for `resume_token` / `fetch_capability` while keeping secrets out of passive surfaces — `src/lib.rs`; `src/main.rs`; `tests/service_contract.rs`; `README.md`; `START_HERE.md`; `docs/NA-0007_authn_authz_policy_subject_contract.md`; `DECISIONS.md`.

- NA-0008 DONE — qsl-attachments PR #21 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/21) merged (merge SHA `d93eb729642a`, mergedAt `2026-03-29T00:32:55Z`); the repo-local queue now returns to `READY=0` because the operator-scoped authn/authz / policy-subject contract is implemented explicitly enough and no direct qsl-attachments-local auth/policy finalization gap remains — see `NEXT_ACTIONS.md`.

- NA-0009 READY — Durability / Recovery Contract — promoted as the sole READY item after `NA-0008` because the operator-scoped authn/authz / policy-subject contract is now implemented explicitly enough, while the next repo-local blocker is the missing durability / recovery contract across crash consistency, restart behavior, backup/restore, and bounded operator recovery expectations — see `NEXT_ACTIONS.md`.

- NA-0009 implementation — repo-local docs now freeze the durability / recovery boundary explicitly: `docs/NA-0009_durability_recovery_contract.md` defines one operator-managed local storage root as the entire durability boundary, treats graceful same-root restart as in scope, limits supported backup/restore to cold whole-root copies plus matching service configuration, and records that abrupt-crash/open-session survival and cross-file transactional durability are not promised while bounded operator recovery may discard orphaned or incomplete local state rather than invent successful delivery semantics — `README.md`; `START_HERE.md`; `DECISIONS.md`; `docs/NA-0009_durability_recovery_contract.md`; `src/lib.rs`; `tests/service_contract.rs`; `tests/NA-0003_constrained_host_validation_evidence.md`; `tests/NA-0004_reference_deployment_validation_evidence.md`; `tests/NA-0005_stress_soak_chaos_evidence.md`; qsl-protocol `docs/canonical/DOC-CAN-006_QATT_Attachment_Service_Contract_v0.1.0_DRAFT.md`; qsl-protocol `docs/design/DOC-ATT-002_qsl-attachments_Deployment_and_Operational_Hardening_Contract_v0.1.0_DRAFT.md`.

- NA-0009 DONE — qsl-attachments PR #23 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/23) promoted the repo-local durability / recovery contract lane, PR #24 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/24) froze the contract without runtime changes, qsl-protocol PR #593 (https://github.com/QuantumShieldLabs/qsl-protocol/pull/593) recorded the cross-repo governance truth, and the repo-local queue now advances to `NA-0010` as the sole READY item because deterministic implementation of the frozen contract is now the next blocker rather than more finalization or continued-support governance — see `NEXT_ACTIONS.md`.

- NA-0010 implementation — the frozen durability / recovery contract is now explicit on runtime and operator surfaces: `src/lib.rs` performs bounded startup reconciliation on the same storage root so only coherent open sessions and committed objects are re-exposed, `src/main.rs` emits the matching operator-safe durability/recovery summary, and `tests/service_contract.rs` now proves graceful same-root restart, paired-file committed-object recovery, fail-closed discard of incoherent recovery artifacts, and repo-local docs truthfulness without changing attachment-service semantics — `src/lib.rs`; `src/main.rs`; `tests/service_contract.rs`; `README.md`; `START_HERE.md`; `docs/NA-0009_durability_recovery_contract.md`.

- NA-0010 DONE — qsl-attachments PR #26 (https://github.com/QuantumShieldLabs/qsl-attachments/pull/26) merged (merge SHA `4a6b2c4eee7a40b64b050859a760e2e72b858911`, mergedAt `2026-03-29T02:19:04Z`); the repo-local queue now advances to `NA-0010A` as the sole READY item because the merged durability / recovery implementation is complete enough that post-merge validation + cleanup is the next blocker rather than another direct implementation gap — see `NEXT_ACTIONS.md`.

- NA-0010A READY — Durability / Recovery Validation + Cleanup — promoted as the sole READY item after `NA-0010` because the merged durability / recovery lane now needs post-merge validation/evidence cleanup rather than more direct implementation work — see `NEXT_ACTIONS.md`.

- NA-0010A implementation — post-merge validation/cleanup now records the merged durability / recovery lane explicitly without changing runtime semantics: `README.md` and `START_HERE.md` now state on the same operator-visible surfaces that hot/live backup and partial restore remain unsupported, `tests/NA-0010A_durability_recovery_validation_evidence.md` records the merged-lane validation matrix truthfully, and `tests/service_contract.rs` proves those docs/evidence surfaces still match the frozen contract while secret-safe audit-handle coverage remains intact — `README.md`; `START_HERE.md`; `docs/NA-0009_durability_recovery_contract.md`; `tests/NA-0010A_durability_recovery_validation_evidence.md`; `tests/service_contract.rs`; `DECISIONS.md`.
